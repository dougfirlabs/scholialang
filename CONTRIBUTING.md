# Contributing

Scholia language changes start in `scholialang-spec`. Update the spec
and fixture corpus first, then update this reference implementation and
its conformance tests.

Before opening a PR:

```sh
pip install -e '.[dev]'
pytest
```

